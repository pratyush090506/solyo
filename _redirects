# Redirect /index.html to root
/index.html / 301

# Redirect www to non-www (canonical)
https://www.trysolyo.netlify.app/* https://trysolyo.netlify.app/:splat 301!

# Redirect .html and .php to root (optional if you use clean URLs)
# Remove if not needed
/*.html /:splat 301!
/*.php /:splat 301!

# Custom 404 page
/* /404.html 404
